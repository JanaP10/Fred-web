const languageButtons=document.querySelectorAll('.lang-btn');const translatable=document.querySelectorAll('[data-en][data-fr]');const editable=document.querySelectorAll('.editable[data-edit-key]');let currentLang=localStorage.getItem('fred-language')==='fr'?'fr':'en';let editing=false;function storageKey(el,lang){return`fred-edit:${el.dataset.editKey}:${lang}`}function setLanguage(lang){currentLang=lang;document.documentElement.lang=lang;translatable.forEach(el=>{const saved=el.dataset.editKey?localStorage.getItem(storageKey(el,lang)):null;el.textContent=saved??el.dataset[lang]});languageButtons.forEach(btn=>btn.classList.toggle('active',btn.dataset.lang===lang));localStorage.setItem('fred-language',lang)}languageButtons.forEach(btn=>btn.addEventListener('click',()=>setLanguage(btn.dataset.lang)));setLanguage(currentLang);document.getElementById('year').textContent=new Date().getFullYear();const editToggle=document.getElementById('edit-toggle');const editBar=document.getElementById('edit-bar');function setEditing(on){editing=on;document.body.classList.toggle('editing',on);editToggle.classList.toggle('active',on);editBar.hidden=!on;editable.forEach(el=>{el.contentEditable=on?'true':'false';el.spellcheck=on});}editToggle.addEventListener('click',()=>setEditing(!editing));editable.forEach(el=>{el.addEventListener('input',()=>{if(editing)localStorage.setItem(storageKey(el,currentLang),el.textContent.trim())});el.addEventListener('blur',()=>{if(editing){const value=el.textContent.trim();localStorage.setItem(storageKey(el,currentLang),value);el.dataset[currentLang]=value}})});document.getElementById('reset-edits').addEventListener('click',()=>{editable.forEach(el=>{localStorage.removeItem(storageKey(el,'en'));localStorage.removeItem(storageKey(el,'fr'))});setLanguage(currentLang)});document.getElementById('booking-form').addEventListener('submit',e=>{e.preventDefault();const note=document.getElementById('form-note');note.textContent=currentLang==='fr'?"Merci ! Ceci est encore une démo. Nous connecterons l'envoi réel et le calendrier à l'étape suivante.":'Thank you! This is still a demo. We will connect real submission and the calendar in the next step.';note.style.fontWeight='700';});