const login=document.getElementById('login'),editor=document.getElementById('editor'),status=document.getElementById('status');let password='';document.getElementById('login-form').addEventListener('submit',async e=>{e.preventDefault();password=document.getElementById('password').value;status.textContent='Checking…';const r=await fetch('/api/admin',{headers:{'x-admin-password':password}});if(!r.ok){status.textContent='Incorrect password.';return}const d=await r.json();for(const [k,v] of Object.entries(d.prices))document.querySelector('[name='+k+']').value=v;login.classList.add('hidden');editor.classList.remove('hidden');status.textContent='';});document.getElementById('price-form').addEventListener('submit',async e=>{e.preventDefault();status.textContent='Publishing…';const prices=Object.fromEntries(new FormData(e.target));const r=await fetch('/api/admin',{method:'POST',headers:{'content-type':'application/json','x-admin-password':password},body:JSON.stringify({prices})});const d=await r.json().catch(()=>({}));status.textContent=r.ok?'Published. The public site will update shortly.':(d.error||'Could not publish.');});